const cards=[['Testing Risk','Risk','#F2A93B','Project reliability requires attention. Increase automated testing and investigate the modules generating the highest defect rate — currently payments-service and auth-module.','bug_rate > 20% AND test_coverage < 60% THEN flag_reliability_risk()'],['Velocity Improvement','Positive','#33D9BE','Development velocity is improving. Sprint 14 closed 18% more story points than Sprint 13, while average task completion time dropped by 22%.','tasks_completed ↑ AND avg_task_time ↓ THEN flag_velocity_gain()'],['Documentation Gap','Risk','#FF6B6B','Three modules merged in the last sprint have no accompanying README or inline documentation.','merged_modules_undocumented > 2 THEN flag_documentation_gap()'],['Code Quality Strength','Positive','#33D9BE','Code quality score has held above 88 for four consecutive sprints. Static analysis flags are down 40%.','code_quality > 85 FOR 4_sprints THEN flag_quality_strength()']];
export default function Insights(){return <><div className="intro"><div className="panel-title">Engineering Intelligence Engine</div><div className="panel-sub">Recommendations are generated from deterministic rules applied to your project data — every card shows the condition that triggered it.</div></div><div className="insight-grid">{cards.map(([title,severity,color,body,rule])=><div className="card insight-card" style={{borderLeftColor:color}} key={title}><div className="insight-head"><div className="insight-icon" style={{background:`${color}24`,color}}>✦</div><div className="insight-title">{title}</div><span className="severity" style={{background:`${color}24`,color}}>{severity}</span></div><div className="insight-body">{body}</div><div className="rule"><span className="kw">IF</span> {rule}</div></div>)}</div></>}
