"use client";
import {useState} from "react";import Link from "next/link";
export default function AdminLogin(){const [adminId,setAdminId]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{const response=await fetch('http://127.0.0.1:8000/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({admin_id:adminId,password})});const data=await response.json();if(!response.ok)throw new Error(data.detail||'Admin authentication failed');localStorage.setItem('admin_token',data.access_token);localStorage.setItem('admin_user',JSON.stringify(data.user));window.location.href='/admin'}catch(error){setError(error instanceof Error?error.message:'Admin authentication failed')}finally{setLoading(false)}}return <div className="auth-screen"><div className="auth-card"><Link href="/dashboard" className="auth-brand"><span className="auth-mark">◈</span><span>DevFlow <small>ADMIN CONSOLE</small></span></Link><div className="card-label">Restricted area</div><h1>Admin sign in</h1><p className="auth-copy">Use your admin email and password to access workspace controls.</p><form onSubmit={submit} className="settings-form"><label>Admin email<input className="field" type="email" value={adminId} onChange={e=>setAdminId(e.target.value)} placeholder="admintesting@gmail.com" required/></label><label>Password<input className="field" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your admin password" required/></label>{error&&<div className="error">{error}</div>}<button className="primary-btn" disabled={loading}>{loading?'Verifying…':'Sign in to Admin Panel'}</button></form><Link href="/login" className="auth-back">← Return to member sign in</Link></div></div>}
